import React from 'react'
import ReactDOM from 'react-dom'
import App from './App.js'

//Component - A function that return JSX (html)







//First argument is what we want to input, second argument is where do we want to render
ReactDOM.render(<App />, document.getElementById('root'))

